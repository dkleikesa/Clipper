# 更新日志

## [1.2.0] - 2026-09-25

### 新增

- **应用新增 CLI 跨进程通信支持**：Clipper 运行期间在 `~/.clipper/clipper.sock` 上监听本机请求，交给命令行与 AI 助手调用
- **`clipper` 命令行工具**：Kotlin/Native 原生二进制（约 2.2 MB，无 JDK 依赖），提供 `list` / `search` / `get` / `copy` / `pin` / `unpin` / `delete` / `stats` / `ping` 九条命令；命令调用的是运行中的应用，因此需 Clipper 正在运行
- **AI skill 随包发布**：release 资产新增 `clipper-skill-<版本>.zip`，内含 `SKILL.md`、协议参考与配套版本的二进制，放入 skills 目录即可使用
- **「允许访问剪贴板历史」开关**（设置 → AI 服务 → 命令行工具）：默认开启，可随时撤销授权；关闭后除探活 `ping` 外，各命令均返回「已关闭授权」

### 安全

- 本机命令行通道仅当前用户可连：`~/.clipper/clipper.sock` 所在目录权限 `0700`、socket 权限 `0600`

## [1.1.0] - 2026-09-25

### 新增

- **设置改为独立窗口**：左栏分区导航 + 右栏内容各自滚动，标题栏自绘以跟随深色主题；不再是面板里那张被夹住的长卡片
- **面板内所有快捷键均可录制**：包括导航、四种激活、`⎋`、`⌘,` 与数字键，支持清除绑定与一键恢复默认，每行带一句说明
- **激活的四种按法各占一条快捷键**：激活 `⌥⏎`、去格式激活 `⌥⌘⏎`、直接粘贴 `⏎`、去格式粘贴 `⌘⏎`；鼠标单击条目做什么也可单独指定
- **`⌘1`…`⌘9` 快速粘贴**前 9 个置顶项
- **历史列表多选**：`⇧` 连续选中、`⌘` 点击多选、`⌘A` 全选，可合并复制或逐条粘贴，右键菜单提供同一组操作

### 变更

- 移除「自动粘贴」「粘贴时去除格式」两个开关，其含义改由四条独立快捷键表达
- 取消录制的方式改为 `⌘.` 或再点一次该行，`⎋` 因此也能被录成快捷键
- 置顶区顺序改由**置顶时间**决定：不再随排序设置或使用（粘贴、再复制）而变，`⌘1`…`⌘9` 对应的条目因此固定下来

### 修复

- 预览与合并复制改用完整文本，不再被标题的 1000 字符截断

## [1.0.0] - 2026-09-24

首个正式版本。

**剪贴板历史**

- 文本 / 图片 / 文件 / 富文本（HTML / RTF）记录，保留原始格式，粘贴不丢格式
- 密码管理器声明为「临时 / 机密 / 自动生成」的内容始终不记录
- 历史上限（默认 1 万条，不设上限）、超限淘汰、暂停记录、退出时清空、一键清除

**查找与整理**

- 五档打分搜索：精确 / 整词 / 前缀 / 子串 / 子序列，多词 AND，命中高亮样式可选
- 正文深搜：结果滚到底后继续在正文里找，按批读取不常驻内存
- 类型筛选栏（文本 / 图片 / 文件 / 富文本）与四种排序（最后复制 / 首次复制 / 复制次数 / 内容大小，支持升降序）
- 置顶，可固定在列表顶部或底部

**外观与集成**

- 图片文字识别（OCR），识别结果作条目标题、可一键复制原文
- 预览面板：宽度可拖拽、开合状态持久化、展示来源应用与元信息
- 主题三模式（跟随系统 / 浅色 / 深色）、弹窗位置与显示器可选、面板尺寸自动或自定义、开机自启、不占用 Dock
- 全局热键呼出面板、托盘图标开关、`⌘P` 暂停记录
- 显示开关：十六进制色块、特殊符号、来源应用图标、图片最大高度

[1.2.0]: https://github.com/dkleikesa/Clipper/compare/v1.1.0...v1.2.0
[1.1.0]: https://github.com/dkleikesa/Clipper/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/dkleikesa/Clipper/releases/tag/v1.0.0
